- hosts: localhost
  remote_user: root
  roles:
    - {
        role: static-deploy,
        application: "static-deploy",
        linked_sample_files: [ 'babel_settings.js', 'config/secrets.js' ],
        sample_extname: 'example'
      }
